# Lab 16 — Agent Arena
# Mock path (default, graded, offline): pure stdlib + pytest. No network,
# no API key, no downloads required — this is a course fairness rule as
# well as a convenience: a student whose API key fails must still be
# able to compete on the mock model.
pytest>=8.0,<9.0

# Real-provider path (optional, NOT required to compete): uncomment if
# you want to point the harness at a live model instead of arena's
# mock. Pin whichever SDK matches the provider you use.
# anthropic>=0.40,<1.0
# openai>=1.50,<2.0
